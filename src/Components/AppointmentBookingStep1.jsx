import React from 'react';
import ServicesSelection from './ServicesSelection';

const AppointmentBookingStep1 = ({ serviceType }) => {
  return <ServicesSelection serviceType={serviceType} />;
};

export default AppointmentBookingStep1;